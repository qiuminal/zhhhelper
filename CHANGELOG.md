# 虎助手 · 更新日志（详细版）

> 客户端「关于」页仅展示精炼文案，本文件保留仓库内详细记录，便于后续追溯与维护。

## 0.3.0（2026-09-09）
· 新增文章跟打 Beta 版：支持从剪贴板载文或导入手机 TXT 文件进行跟打
· 文章跟打区沿用练单区的字帖模式：正文即跟打区，中文两端对齐，逐字即时校验
· 实时统计 速度/字数/错字，支持重打与成绩记录
· 成绩历史持久化表格展示，最多保留 100 条
· 跟打区提前一拍滚动，光标进入可视区倒数第二行即翻出下一行，消除页末等待
· 成绩表格只绘内部框线并铺满整页行，列顺序/字号/配色与练单保持一致；修复时间列被挤成两行、行首光标与首字重叠、冷启动成绩表空白等细节问题
· 成绩历史改用应用私有文件 + fsync 落盘，并关闭系统云备份（allowBackup），避免旧快照覆盖私有成绩数据
· 侧边菜单进入「关于」页不再二次重排字体，更新检查延后到页面入场后启动，减少卡顿
## 0.2.6（2026-09-07）
· 优化连续查询时的键盘交互：在 MainActivity.dispatchTouchEvent 的 ACTION_DOWN 阶段判断触点位置；搜索框持有焦点时，点击查询结果卡片、上下滑动列表、点击按钮或搜索框外空白区域，都会让搜索框清除焦点并通过 InputMethodManager 收起软键盘，避免键盘持续遮挡查询内容
· 搜索框和清除按钮保持输入优先：抽出 isTouchInside 统一进行窗口坐标命中判断，点在搜索框内不干预输入；清除按钮作为明确例外，不触发框外收键盘逻辑
· 点击清除后执行清空、requestFocus 与 showSoftInput，光标继续停留在搜索框且键盘保持/恢复显示，用户可直接输入下一个字，不增加重复点击步骤
· 查询仍沿用文字上屏后实时触发的原有逻辑，不添加搜索键、确认键或任何额外查询步骤

## 0.2.5（2026-09-06）
· 拆分栏排版重做：新增 RootPairsView 自定义 ViewGroup，每组「大写+小写」两字母字根编码居中悬于对应部件正上方；以配对单元为最小换行单位，字根较多或用户调大字号导致换行时，编码始终跟随其部件，对应关系不断裂（查询结果卡片 / 错字速查弹窗 / 分享图片三处同步，字号随用户设置缩放：部件 sp、编码 sp-4）
· 新增 parseRootPairs 纯函数（UiFormat.kt）：字根码组数与部件码点数严格配对后才逐组对齐；全码表 99,138 条中 99,137 条可配对，唯一例外 𠔻（20 部件对 18 码，码表数据本身不齐）自动降级为旧版两行样式，不会错位显示；UiFormatTest 补 6 项单测（连写部件 / 增补平面 / PUA / 数量不配 / 空值），累计 12/12 通过
· 修复多字体部件纵向错位：TumanPUA（upem=256，ascent 0.859em）与霞鹜文楷（upem=2048，ascent 0.928em）等内置字体度量不同，部件拆为独立视图按顶边堆叠后，PUA 部件整体偏上（如「攘」的部件 U+E417）；改为行内统一基线对齐（按 TextView.getBaseline 聚合行 ascent/descent），复刻旧版单 TextView 同基线排版，原则同 CharLabels.glyphCenterOffset 的统一基准度量
· 「整句码：」标签统一改为「整句：」（layout_result_card / layout_char_popup / layout_share_card 三处布局），与「字头 / 编码 / 拆分 / 拼音」等两字标题对齐
· 码表：data/chai.txt 为 㞢（U+37A2）补充拼音 zhī（与源码表全文比对，99,144 行仅此一处差异），tables.bin 已随构建重新生成

## 0.2.4（2026-09-05）
· 质量：为版本比较、拼音/U码展示、键准/速度/击键公式、Levenshtein 编辑距离等纯逻辑补齐 JVM 单元测试（14 项），抽出 UiFormat/PracticeMath 纯函数，便于回归
· 质量：release 开启 R8 代码压缩与资源瘦身（minifyEnabled + shrinkResources），APK 由约 40.8MB 降至约 38.2MB
· 质量：移除逐键 Log.d 与 printStackTrace 等遗留调试输出；删除重复的 formatPinyin/formatUnicode/dp 实现与无用局部变量
· 治理：新增 GitHub Actions（push/PR 自动执行单元测试与 Debug 构建并上传产物）；新增 CONTRIBUTING 与 Issue/PR 模板；.gitignore 忽略构建期自动生成的 tables.bin/keystrokes.bin/labels.bin
· 修复：首位编码高亮时左侧圆弧被裁切（对称内边距 + 负外边距，文字视觉起点不变，圆弧叠加在中文冒号后的空白上）

## 0.2.3（2026-09-05）
· 拆分查询新增「历史搜索」：自动记录已收录字的查询，分次上屏的记录会分别入库；支持展开/收起、睁眼隐藏/显示、一键清空，最多保留最近 50 条
· 查询结果字头新增标签展示（内置标签码表，5 类配色）：命中标签编码的编码在编码栏以对应颜色高亮，点击标签闪烁提示对应编码；错字速查与分享图片同步生效
· 修复「〇」等个别已收录字符查询不到的问题；搜索无结果时提示「找不到结果」；历史记录忽略未收录内容，混合长串仅保留可查部分
· 字头汉字取消加粗（错字速查同步），笔画繁多的生僻字显示更清晰
· 内置标签码表（label.txt）上线，单字码表（zi.txt）更新，并重新生成二进制 tables.bin / labels.bin
· 「形码编码与拆字查询」等文案统一为「拆分查询与跟打练习」
· UI 打磨：标签高亮改为 6dp 圆角矩形，并修复最末位编码高亮右端圆弧被裁剪的问题

## 0.2.2（2026-09-02）
· 练单新增错字速查：本组完成后，错字以蓝色虚线下划线展示，点击即可查看该字的拆分查询卡片（编码/拆分/拼音/U码/整句码），点击空白处关闭
· 新增应用内更新：关于页显示「有新版本」徽标，点击即可下载并安装 GitHub 最新版
· 练单打完最后一组后，可一键「重新开始」或「乱序开始」再练一遍
· 更新整句码码表：新增 档、匕、簇、轼、糖、汽 等字
· 更新日志排版优化：版本标题加粗、正文两端对齐、标点避头尾、条目格式统一

## 0.2.0（2026-09-01）
新增「练单」功能（字帖模式）：
· 内置六套练习文本：前500 / 中500 / 后500 / 前1500 / 常用词1500 / 要你命4000，纯离线跟打
· 发文区即跟打区，手机端体验更友好，逐字即时校验：打对变绿、打错标红
· 实时统计 速度（字/分）/ 击键（键/秒）/ 键准
· 支持全体乱序 / 恢复顺序，冷启动保留乱序状态与跟打进度
· 支持字号调节、错字重打、重打本组，每组完成自动复制成绩到剪贴板，方便分享成绩至社交平台
· 支持外接键盘热键：完成本组跟打后，敲击 空格=下一组、F3=重打本组
· 「字帖模式」之产品概念、部分练习文本与分组规则来自开源项目 TypeSunny（晴跟打，GPL-3.0）

## 0.1.0（2026-08-29）
· 虎助手首个Release版本，内置虎码全字集码表、纯离线查询
· 支持多字查询，查询结果含编码、拆分、拼音、Unicode、整句码（单字在虎整句中的打法）
· 单字查询结果支持图片分享到微信、QQ等应用
· 支持字统/汉典等外部词典一键直达当前字的查询页
· 全局字体：TumanPUA、霞鹜文楷、遍黑体P1/P2，生僻字与拆分部件均可正常显示（分享到QQ和微信也能正常展示）
· 支持字号调节并对分享图字号生效
